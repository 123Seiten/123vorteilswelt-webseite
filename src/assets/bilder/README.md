# src/assets/bilder/

Inhaltsbilder — werden über `astro:assets` (`<Image>`) eingebunden und
beim Build automatisch optimiert (Größe + modernes Format).
Jede Datei hier braucht einen Eintrag in `src/config/bilder.ts`.
